# Baked into the bundle at build time — changing these needs a rebuild, not just a restart.
# Production: point both at the Render service, e.g. https://pm-api.onrender.com
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
